{
  "name": "reverse-proxy-worker",
  "version": "0.2.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler pages deploy .",
    "dev": "wrangler pages dev .",
    "start": "wrangler pages dev ."
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.4.5",
    "vitest": "1.5.0",
    "wrangler": "^3.60.3"
  },
  "dependencies": {
    "reverse-proxy-worker": "file:"
  }
}